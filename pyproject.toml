[project]
name = "mihomo-update"
version = "0.2.0"
description = "Update mihomo proxies from subscription"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0.3",
    "requests>=2.32.5",
]

[project.scripts]
mihomo-update = "mihomo_update.cli:main"
